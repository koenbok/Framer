box: wercker/nodejs

build:
    steps:
        - npm-install
        - npm-test
deploy:
    steps:
        - script:
            name: builds.framerjs.com
            code: make deploy

# $WERCKER_OUTPUT_DIR